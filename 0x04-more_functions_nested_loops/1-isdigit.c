#include "main.h"

/**
 * _isdigit - Checks for digit.
 * @c: The digit ti be checked.
 * Return: 1 as digit or 0 otherwise.
 */

int _isdigit(int c)
{

	if (c >= 48 && c <= 57)
	{
	return (1);
	}
	return (0);

}
