#include <stdlib.h>

/**
 * mul - Multiplcation of two integers.
 * @a: First integer.
 * @b: Secind integer.
 * Return: Results of the multiplied integers.
 */
int mul(int a, int b)
{
	return (a * b);
}
